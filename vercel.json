{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "bunx convex codegen && bunx convex deploy --cmd 'bun run build'",
  "installCommand": "bun install",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
